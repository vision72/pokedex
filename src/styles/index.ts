// Importing all exports from './colors' module and assigning them to Colors
import * as Colors from './colors';

// Importing all exports from './outlines' module and assigning them to Outlines
import * as Outlines from './outlines';

// Importing all exports from './typography' module and assigning them to Typography
import * as Typography from './typography';

// Re-exporting the imported modules so they can be used in other parts of the application
export {Colors, Outlines, Typography};
